// Write your code here

const num1 = 2, num2 = 31, num;

const multiply = num1 * num2

const random = Math.floor((Math.random() * 10) + 1);

const mode = (num3, num4) => num3 % num4;

function max(num) {
  const max = Math.max(num);
}
